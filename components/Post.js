import React from 'react';
import { Text, View, StyleSheet } from 'react-native';
import propTypes from 'prop-types';

const Post = props => { //name //dp // time// title// 
    return (
        
    );
}

export default Post;

const styles = StyleSheet.create({

});


